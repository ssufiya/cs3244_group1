# CS3244 Group 1 — sarcasm detection: data acquisition, cleaning, EDA, splits
pandas>=2.2
numpy>=1.26
pyarrow>=15.0          # parquet
requests>=2.31         # dataset downloads (00)
matplotlib>=3.8        # EDA figures (03)
seaborn>=0.13
scikit-learn>=1.4      # splits + leakage audit + reference floor (04, 05)
langdetect>=1.0.9      # optional: adjudicates the ambiguous language band (01, 02)

# Only needed by Dataset/scripts/scrapers/ (not required for the pipeline above)
# praw>=7.7            # Reddit API — scrape_reddit_sarcasm.py
